
import { prettynum } from 'ember-civclicker/helpers/prettynum';
import { module, test } from 'qunit';

module('Unit | Helper | prettynum');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = prettynum([42]);
  assert.ok(result);
});

