
import { getCostNote } from 'ember-civclicker/helpers/get-cost-note';
import { module, test } from 'qunit';

module('Unit | Helper | get cost note');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getCostNote([42]);
  assert.ok(result);
});

