
import { disabled } from 'ember-civclicker/helpers/disabled';
import { module, test } from 'qunit';

module('Unit | Helper | disabled');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = disabled([42]);
  assert.ok(result);
});

